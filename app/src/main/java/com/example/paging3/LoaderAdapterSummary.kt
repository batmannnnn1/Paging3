package com.example.paging3

/*

When user started scrolling in your app they show the data...
but what if data not available and user's keep scrolling the app it's not good for your app
so use progress bar for this situation when data is fetching change the progress bar visibility...

Create the layout for progress bar
and then Adapter class which extends the Load State Adapter














 */